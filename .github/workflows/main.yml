name: CI
on: push

jobs:
  terraform:
    runs-on: self-hosted
    env:
         DEV_ENV: ${{ secrets.DEVENV }}
    steps:
      - uses: actions/checkout@v2
      - run: terraform init
        # - run: terraform plan -var-file="uat.tfvars"
      - run: echo $DEV_ENV >> value.txt
      - run: cat value.txt
        #    - run: terraform apply -auto-approve -var-file="uat.tfvars"
