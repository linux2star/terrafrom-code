name: CI
on: push

jobs:
  vpc-creation-for-gcp:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v2
    - run: terraform init
    - run: terraform pla
    - run: terraform apply -auto-approve
    
